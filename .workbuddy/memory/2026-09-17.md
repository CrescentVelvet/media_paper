# 2026-09-17

- 规范统一(用户要求「没必要分两种格式,统一为高端/方法密集型格式」):重写 `AGENTS.md`,废除 Style A/B 双轨,全库只保留一套高端样式。
  - 新增「一.5 统一笔记模板(唯一)」:Hero → 摘要 → 背景动机 → 总体框架 → 逐模块「五件套」(网络结构表/公式/前向流程/内部 Mermaid/监督信号)→ 损失总表 → 训练+推理流程图 → 工程实现细节 → 实验 → 消融 → 同类方法对比 → 应用场景 → 可改进方向 → 总结 → 团队/链接。
  - 新增「一.6 内容深度四项硬要求」——本次统一的核心:思路 / 算法逻辑 / 工程细节 / 同类方法对比,每项给出精度下限(如张量维度级数据流、超参必须带 file:line、对比表维度固定不允许删列、≥3 个同类方法且须含论文 baseline 以外)。
  - 第四节样式规范只留高端样式一套,并明确列出旧 NVIDIA 绿扁平主题为「已废弃,禁止再用」。
  - 第七节 depth 分级改为只约束内容体量,不再与样式挂钩(原先写「Style B 14 节模板」)。
  - 新增第八节「存量笔记迁移策略」:渐进迁移,不做一次性批量重排;纯样式迁移不得借机编造内容;迁移单独 commit。
  - 验证清单按「格式项 / 内容项」重排,补上四项精度要求的勾选项。
- `check.py` 新增报告项 6:样式统一进度 + 旧样式待迁移清单(非失败项)。当前结果 21/63 已统一,42 篇待迁移。
- 顺手修掉 `check.py` 的一个隐藏 bug:index 页脚含生成日期(2026-09-15),导致**每次跨天运行 check.py 都必然 FAIL**,与笔记内容无关。改为比对前把 `更新于 YYYY-MM-DD` 归一化,并把实际生成日期作为报告项输出。
- `README.md` 同步:AGENTS.md 描述改为「统一高端格式 + 四项精度要求」,篇数 62 → 63(实际笔记数),补「看统一格式迁移进度」小节。
- 校验:`python check.py` 通过([OK] 全部通过)。
- 用户决策(AskUserQuestion 两问):① 存量 42 篇旧样式走**渐进迁移**,不批量重排;② 经验分享笔记**改写为统一格式叙事**(非保留历史)。
- 改写 `CV-AI-20260813-用AI做论文调研经验分享.html`(786 行,mermaid/表格标签数已校验平衡):
  - 3.4 由「Style A vs Style B:怎么选」整节换成「统一高端格式 + 四类精度要求」,加规范演进 callout(记 commit c50e67f)。
  - 3.2 模块清单 7 → 8 个(新增「④ 内容精度」,把「③ HTML 样式」改为全库唯一);AGENTS.md 行数 133 → 294。
  - 3.3 验证清单重写为「硬性项(脚本)/真实性项/格式项/内容项/流程项」。
  - 顺带更正事实性过时引用:`knowledge-index.json` **已不存在**(被 papers.json 取代,commit d243f9f),另发现 `papers.json` 的 `anchors` 字段(章节锚点列表,17/63 篇已填)正是该笔记 §9 设想「知识点索引」的雏形 —— 已写入 §9.1。
  - 修正 8.2 里「edit index.html 加 .note-card」的**错误操作步骤**(index 是生成物,应改 papers.json + build_index.py),并把 check.py 补为 commit 硬门槛;8.3 三道门。
  - 篇数口径统一 40/51 → 63(取自 papers.json 实际条目数);修 §9/§10 内部编号错(9.1/9.2 → 10.1/10.2);核心经验 3 条 → 4 条(新增「样式只能有一套」);页脚补「修订于 2026-09-17」。
- `check.py` 又修一处计数缺陷:不确定标注统计把 `<pre>/<code>` 里「讲解标记用法」的说明文字也算进去了(新增的 3.3 清单与 3.4 卡片导致假计数 4 处→9 变 13)。改为先剥离 pre/code 再计数,恢复为真实值 9 处 / 1 篇。
- `papers.json`:给该笔记加 tag「统一高端格式」,`python build_index.py` 重新生成 index(63 卡,173 KB)。
- commit 教训(自己踩的):用 `git add -A` 误将**无关文件**纳入提交 —— `.workbuddy/memory/*`(此前一直未纳入版本管理)与 `images/2607.20789/`(5 张孤儿图,全库无任何笔记引用,疑为 SHARP 调研遗留)。已 `git reset --soft HEAD~1` 撤回重提,只留本任务 4 个文件。**结论:本仓库提交一律列明具体路径,禁用 `git add -A`。**
- 最终提交:c50e67f(规范统一)、c05de7e(笔记同步)。

## 二次检查(16:41):发现并发会话
- 用户说「改完就commit吧」,复查 `git status` 时发现工作区多出**另一会话在飞的活**:`3D-20260722-3D-GIMP.html`(859 行,arXiv 2607.20789,用的是新统一格式)、`figures/2607.20789.json`、`papers.json` 新增条目、`index.html` 重新生成。这几个文件 mtime 为 16:40:37–16:41:04(即我查看前 46–73 秒),**`MEMORY.md` 也被它同时改写**(孤儿图结论、arXiv 配图抓取技法都是它写的)。
- 判断:本仓库可被多会话并发写入,`papers.json`/`index.html`/记忆文件是热点。**因此没有替那个会话提交**,只报告并等用户定夺。
- 更正我此前的错误结论:`images/2607.20789/` 不是「SHARP 调研遗留的孤儿图」,`2607.20789` 就是 3D-GIMP 的 arXiv ID,那 6 张图(新增 `360USID_visual.jpg`)全是该笔记配图。**图片先落盘、笔记后写是正常中间态,不能凭"当前无引用"就判孤儿。**
- 复查 `python check.py` exit=0,当前仓库(64 篇笔记,统一格式进度 22/64)一致。
- 收尾:用户选择「不动,等那个会话提交」,该会话随后自行提交为 `e1ebc6e media_paper: 新增 3D-GIMP 论文笔记`。工作区已干净(仅余未跟踪的 `.workbuddy/`)。**结论:交给产出方提交是对的 —— 避免了两个会话交叉写历史。**
- 新增笔记 `3D-20260722-3D-GIMP.html`(arXiv 2607.20789,《3D-GIMP: When 3D Gaussian Inpainting Meets PatchMatch》,斯图加特大学 VISUS,2026-07-22,15 页):
  - 统一高端样式,13 节(含 TOC 锚点 s1–s13),Mermaid 图 2 张、公式 11 处(泊松深度补全 / PatchMatch 代价 / SH 加权最小二乘)、表格 6 张、本地配图 6 张。
  - 核心结论:范式论文而非性能论文 —— 「只在一帧参考视图跑一次生成修复,其余靠 3D-aware PatchMatch 几何传播」,用确定性对应消灭 hallucination drift。
  - **三个可独立拆出的组件**(笔记 §11 做了对本库 pipeline 的迁移分析):① 3D 包围盒投影 + guard band(s=1.2)生成跨视图一致掩码,**替代逐帧 2D mask**(论文明批评 SAM 类逐帧分割「inherently prone to multi-view inconsistency」——直接命中本库 SAM3 track 去重痛点);② 梯度域泊松深度补全(3DGS 光栅化深度做 Dirichlet 边界锚定单目深度尺度,s_f=0.1);③ SH 系数约束加权最小二乘预计算。
  - **事实校正(避免被摘要误导)**:IMFine 上 2DGS 版 PSNR 19.09 < IMFine* 的 19.67、LPIPS 0.248 也劣于 AuraFusion360 的 0.173,真正优势是 FID 53.11 与 6 min;**360-USID 上 PSNR 15.02 输给 AuraFusion360 的 16.22**;换回原生 3DGS 后 FID 劣化到 83.06。摘要的 "competitive" 准确,「全面 SOTA」不成立。
  - 已标 2 处【待核实】+ 1 处【推测】:Table 2 的 LeftRefill 行(16.22/0.445/61.03)与 Table 1 中 360-USID 的 AuraFusion360 行**数值完全相同**;正文写 "Gemini 2.5" 而参考文献写 "gemini 3 pro";「固定表情系数的替代思路」属无原文佐证的推断。
  - 无公开代码 / 项目主页。
- `images/2607.20789/` 破案:该目录**就是 3D-GIMP 的配图**,此前 MEMORY.md 误判为「SHARP 调研遗留的孤儿图」。本次本地化 6 张(teaser_v3 / hallucinations / dataflow / patch_propagation_v2 / res_viewdepent_effects_v3 / 360USID_visual),全部被新笔记引用,不再是悬空图片 → 已更正 MEMORY.md 第 2 节。`figures/2607.20789.json` 同步记录图号、原始 URL 与图注。
- `papers.json`:新条目插在 `3d` 分区 index 24 之后(ArtiFixer / 4DAnyone 之后),kind=note、depth=deep、topics=[几何与时序一致性, 表面与几何重建]。`python build_index.py` → 64 卡 / 176 KB;`python check.py` → [OK] 全部通过(统一格式进度 22/64)。
- 工具链:本会话 Bash 工具 PATH 损坏(`ls`/`head`/`dirname` 均 command not found),改用 **PowerShell + 托管 Python 全路径**;且 PowerShell 的 stdout 不回显,一律**让脚本把结果写到 UTF-8 文件再用 Read 读**(注意 `*>` / `Out-File` 会写成 UTF-16 被判定为二进制)。
