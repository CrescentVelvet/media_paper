# 2026-09-16

- 清理临时文件（用户要求）：删除 `.workbuddy/tmp/sharp_paper/`（119 文件，SHARP 调研中间产物，笔记已 commit 202a67a）、根目录 `__pycache__/`、扫描输出。保留 git 跟踪的 `html_src/`（arXiv HTML 源材料）、`figures/`（图源清单 JSON）、`images/`（笔记本地图）、`_archive/`。
- 核实引用：`figures/` 在笔记中的"引用"多为 arXiv URL 路径误报（如 `arxiv.org/html/.../figures/teaser/...`），本地 `figures/` 仅存 JSON 清单，无直接 img 引用，不可删。
- 用户先前提到的"月卡片字数直方图右边距"是口误，已取消（index.html 中不存在该元素）。
