# 2026-09-20 Lyra 2.0 + OctWorld 成对调研(任务闭环)

## 产出
- `3D-20260414-Lyra2.html`(deep,17 节、6 Mermaid、9 张本地图),arXiv 2604.13036,NVIDIA SIL,无会议 badge(comments 仅 Project Page)。
- `3D-20260903-OctWorld.html`(deep,17 节、4 Mermaid、12 张本地图),arXiv 2609.03919,USTC+MSRA+清华,**ECCV 2026**。
- papers.json 插在 Lyra 1.0 之后(3d 区),build_index **68 篇**,check.py [OK]。
- commits:`390c581`(两篇笔记+papers+index+19 张图)、`61d7711`(配图引用补齐与图号重排)、`e6b9a3f`(首帧深度图补全下载)。

## 为什么成对做
两篇解**完全相同的问题**(单图沿长轨迹自回归、重访区域保持一致)、**答案方向相反**:Lyra 2.0 明确拒绝全局融合(逐帧独立存几何、只做信息路由),OctWorld 正面拥抱全局融合(OctMap 八叉树+TSDF)。两篇都 **0 次互引**,且 OctWorld-1.5 与 Lyra 2.0 用**同一骨干 Wan 2.1-14B I2V** —— 同骨干同期独立收敛、解法相反,是本子方向最有信息量的对照。唯一未被任一方做的实验:同骨干同协议下比 ①逐帧 cache+路由 ②OctMap 融合 ③无记忆(+轨迹长度 vs 指标曲线)。

## 源码验证(仅 Lyra 2.0 有代码)
- 仓库 `nv-tlabs/lyra` 的 `Lyra-2/` 子目录,源码全(lyra2_model.py 144KB、schedulers/self_forcing_scheduler.py、scripts/train.py、TRAINING.md)。
- **论文 vs 代码两处差异**(已写入笔记 §10):① `self_aug_prob` 论文 0.7 / 代码 1.0;② `weight_decay` 论文 0.1 / 代码 1e-3。
- ③ TRAINING.md 说 64×B200 约 5000 步收敛,论文写 64×GB200 / 7000 步 —— 硬件与步数都不一致。
- ④ 代码支持多步 self-augmentation 采样,论文只说"一次额外 DiT 前向"(配置最终取 1 步)。
- 关键实现确认:`wan2pt1_lyra2.py:183-198` 的 `inject_kq_only` 精确对应论文"注入 q/k 不改 value";canonical 坐标公式与论文一致。
- **OctWorld 无代码**:项目页 `Code (Coming Soon)` 且 href 指向 `#top`,第三方站(CatalyzeX)的 View Code 是占位。§9 工程细节全部来自论文附录、**无源码级验证**,笔记内已显式声明可信度等级低于 Lyra 2.0。

## 本轮新踩的坑
1. **配图下载漏文件不报错**:批量下载脚本只落了 3 张(子目录图被扁平化后名字对不上),其余静默缺失;核对脚本用「笔记内 src 全集 vs 目录文件全集」双向 diff 才查出 7 张缺 + 2 张孤儿。**结论:下载后必须做双向 diff,不能只看"下载脚本说 OK"**。
2. **孤儿图处理**:`action_model.png`(Figure I(b)) 与 `firstdepth.png`(Figure II) 下载了但未引用 → 选择**挂进已有对应段落**而非删除(§11.4 动作条件对比、§9.6 首帧深度),比删掉更有价值。
3. **图号重排**:中途插入新图会让 `图N` 与文档顺序脱节。做法:正则 `<b>图(\d+)(\(` 捕获图注、用映射表批量替换(避免链式碰撞),替换后校验位置序列严格递增 + 无重号 + 交叉引用同步。**注意:笔记内可能已有同题材图(本次 §15 的图10 就是同一张 invsible.png),插入前先查重**。
4. **stat 卡的孤立错误**:OctWorld stat 卡写"3.1–2.0× 重投影 PSNR 提升",但 23.47 vs 22.08 只差 +1.39 dB,论文全文无任何 3.1× 表述 → 已改。**教训:stat 卡这类"提炼数字"最容易脱原文,核对优先级应高于正文表格**(正文表格本次逐格对比全部命中)。
5. **papers.json summary 的隐形约束**:全库其余 67 条 summary 只用 `<strong>`,**无 `<a>`、无 LaTeX**。新写的两条原本带了链接与 `$...$`,已改为纯文本以保持一致(避免 index 卡片渲染异常)。
6. **宿主注入的 current_time 可能过时**:本次会话跨天(09-18 起),注入值是 09-18 而系统时钟已是 09-20;index.html 生成日期与笔记内"截至本笔记撰写"日期都用系统时钟,两者一致。**判断"今天是哪天"以系统时钟为准**。

## 检索侧的经验(上一轮)
- 用 WebSearch 探路时曾把 Lyra 1.0 的 ICLR 2026 误挂到 2.0 上 —— **会议信息必须回 abs 页 comments 字段核实**,不能靠同系列推断。
