# 2026-09-21

## PERSIST(arXiv 2603.03482)深度笔记入库 ✅ commit db8ceb5
- 笔记:`3D-20260303-PERSIST.html`,17 节 deep,12 个编号图 + 16 张网格子图(fig_* 4 模态卡、h2h 4 联、lim 8 帧),28/28 图全挂载,零缺失零孤儿。
- 论文:PERSIST(ICML 2026,Mila×UdeM×McGill),持久 3D 状态(48³ 体素 world-frame latent)替代像素历史/关键帧检索;三组件 W_θ(rectified flow DiT)/C_θ(相机残差)/P_θ(learned deferred shader)训练独立推理组装;可微投影算子 R(nvdiffrast depth-peeling, max 32 层)。
- 主结果:FVD@600 = 148(Oasis 875,+15% vs +114% 恶化);+w0 显式初始化 104;人评 Overall 3.0 vs Oasis 1.9;PERSIST-S 2-step 886ms/帧。
- **「源码印证论文」案例**:论文 Table 9 超参(σ_min=1e-5/20 步/η=3/τ_ctx=0.02W+0.1P/10% 噪声增强)与发布 `pipeline.json` 逐项吻合,无矛盾。动作 23 维、相机 10 维、`worldmem_cam_to_minetest_cam` 相机对齐函数均实测确认。
- papers.json 插在 CovRAG 之后(记忆检索/3D 世界模型赛道聚类:Oasis→WorldMem→CovRAG→PERSIST);README 篇数 70→71、deep 23→24;check.py 全过(0 死链)。

### 踩坑与经验
- **图号批量重排的中间态陷阱**:先挂 18 张孤儿图时新编图号与存量图号交错冲突(两个重复 8、两个重复 10),按文档顺序逐一 Edit 修正后才严格递增。教训:**中途插图前先把目标图号序列完整规划好再动手**,不要边挂边编。
- **callout 内误用 `</p>` 闭合**:callout div 里写了 `</p>` 导致标签配平差点错位,校验脚本必须含标签配平项(section/div/table)。
- 校验脚本全套:图引用双向 diff + `<b>图(\d+)</b>` 严格递增无重号 + toc 锚点 + 内链存在性 + 标签配平,一次跑齐再提交。
