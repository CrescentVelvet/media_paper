# AGENTS.md — 论文调研与分析笔记编写规范

> 本文件是知识库 `media_paper` 的论文调研作业规范。当被要求"调研某 arXiv/GitHub 链接、提取图像、绘制框图、分点解析"时,严格按此执行。
> 所有分析笔记为**自包含单文件 HTML**(`lang="zh-CN"`),无构建步骤,浏览器直开。
> **格式唯一**:全库只保留一套「方法密集型高端格式」(见「四、五」),**不再区分 Style A / Style B**。
> **写任何内容前必读「二、真实性硬约束」;成稿前必须 `python check.py` 通过。**

---

## 一、调研工作流(从链接到成品)

### 1. 输入解析
- **arXiv 链接**(如 `https://arxiv.org/abs/2410.01425`):提取 arxiv-id `2410.01425`。
- **GitHub 链接**(如 `https://github.com/zhenliuZJU/EVA-Gaussian`):提取仓库名作为短名,从 README 找到论文 arXiv 链接与项目主页。
- **项目主页**(`.github.io`):通常含 Abstract、Method Overview 图、可视化结果图。

### 2. 信息抓取(并行发起)
1. **arXiv abs 页** `https://arxiv.org/abs/<id>` —— 取标题、作者、机构、abstract、提交日期。
2. **arXiv HTML 全文** `https://arxiv.org/html/<id>v2` —— **最重要的内容源**,含完整方法、公式、实验、图。注意取最新版本号(在 abs 页查 submission history,v2/v3...)。
   - ⚠️ HTML 全文可能被截断(超 2000 行/51200 字节),输出会落盘到临时文件,用 Task 工具委派 explore 子代理读取并提取关键节(方法/损失/实验/消融),**不要自己 Read 全文**以免爆上下文。
3. **项目主页** —— 取 Method Overview 图、模块图、可视化对比图。
4. **GitHub README** —— 取环境配置、训练命令、数据集说明、网络结构线索。
5. **GitHub 源码(deep 级默认必做,非可选)** —— `git clone --depth 1 <repo> $TEMP` 到临时目录,读 `train.py`/`lib/*.py`/`config/*.yaml` 验证论文叙述、提取真实超参(如损失权重、特征维度、循环次数)。**源码是检验论文"宣称 vs 实现"差异的关键**,也是「⚙️ 工程实现细节」章节的唯一可信来源。
   - 仓库无源码时(README 仅写 "Code coming soon"),**不降级猜测**:在「工程实现细节」章节明确写"仓库无源码,以下超参无法验证",并按「二、2」标注。

### 3. 图像提取(两种策略)
- **策略 A — 热链 arXiv HTML 图**:优先用 `<img src="https://arxiv.org/html/<id>v<n>/x1.png" ...>`。前导注释标记来源:`<!-- arXiv HTML figures (from https://arxiv.org/html/<id>v<n>) -->`。
  - 图文件名序列:x1.png, x2.png, ...;部分用命名图:`.../figures/teaser.png`。
  - 标准内联样式:`style="max-width:100%;border-radius:8px;border:1px solid #333;"` 加 `loading="lazy"`。
- **策略 B — 本地图**(arXiv 图失效或知乎深度阅读图):存到 `images/<arxiv-id>/x1.png` 或 `images/<name>-zhihu/x1.jpg`(知乎图为 .jpg)。用 `<div class="figure"><img src="images/..."><div class="caption"><b>图N</b> 说明</div></div>`。
- **图失效处理**:见 commit `6a0112e Fix SR survey: replace broken image refs with PDF links`——改链到 PDF 下载。
- 注意:「图像策略 A/B」与已废弃的「样式 A/B」无关,不要混用命名。

### 4. 框图绘制(四代技术,按优先级选)
| 技术 | 适用场景 | 示例 |
|---|---|---|
| **Mermaid.js**(首选) | 多阶段 pipeline、模块内部数据流、训练/推理流程 | `3D-20241002-EVA-Gaussian.html` |
| **HTML/CSS 框图**(`.hdiag`/`.hbox`/`.mtree`) | 不依赖 CDN、需轻量嵌入 | `3D-20260720-同事人体重建Pipeline详解.html` |
| **内联 SVG** | 一张"总结性大图",色码分组、复杂连线 | 同上文件 line 1316+ |
| **ASCII `<pre class="ascii">`** | **新文件不要再用了** | 旧文件 4 个,已被 commit `47102e5` 标记迁移 |

**Mermaid 用法**:
```html
<div class="mermaid">
flowchart TD
    INPUT["输入: n 张稀疏视角 RGB 图像"] --> S1
    subgraph S1["阶段一: 3D 高斯位置估计"]
        S1A["AttenUNet"] --> S1B["EVA 模块"] --> S1C["输出: 位置图"]
    end
    style INPUT fill:#dbeafe,stroke:#3b82f6,color:#1e3a5f
</div>
...
<script>mermaid.initialize({startOnLoad:true, theme:'default', flowchart:{curve:'basis', useMaxWidth:true}});</script>
```
- 用 `subgraph` 分组、`style` 节点配色(浅底深字)、`<br/>` 换行、`flowchart TD`(纵向)/`LR`(横向)。
- **一张笔记至少 3 张图**:① 总体数据流图 ② 每个核心模块的内部图(或合并为一张分模块大图) ③ 训练/推理流程图。

### 5. 统一笔记模板(唯一,不再有第二套)
```
0. Hero 头(标题 / 副标题 / 作者机构 / 徽章 / 资源链接)+ 目录(TOC,2 列)
1. 论文摘要                      —— 一段话讲清问题-方法-结果,附一句话核心贡献
2. 研究背景与动机                —— 现有方法局限(.grid-3 .card 卡片)+ 本文核心洞察(insight)
3. 算法总体框架                  —— 输入/输出定义 + 符号表 + 阶段划分 + 总体 Mermaid 图
4~N. 逐阶段 / 逐模块深挖         —— 每个模块固定"五件套",见下
N+1. 损失函数总表                —— 表格:损失项 / 公式 / 权重(真值) / 作用
N+2. 逻辑流程图                  —— 训练流程 + 推理流程(两张 Mermaid)
N+3. ⚙️ 工程实现细节             —— 源码级,见「一.6」
N+4. 实验结果                    —— 量化表格(PSNR/SSIM/LPIPS/FPS/VRAM),最优行 .best、失败项 .fail
N+5. 消融实验                    —— 逐个组件 + Δ 指标
N+6. 🔄 同类方法对比分析          —— 必写,见「一.6」
N+7. 🎯 应用场景与落地适配性      —— 能否进我们 pipeline、显存/耗时/数据成本
N+8. 可改进方向分析              —— 知识库特色章节,见「一.7」
N+9. 总结与演进定位
N+10. 👥 研究团队 + 🔗 资源链接
```

**模块"五件套"(4~N 每节必须齐)**:
1. **网络结构表**:输入张量形状 → 层/算子 → 输出形状(维度必须写全,如 `[B, N, 3]`)。
2. **数学形式化**:`.equation` + `$$...$$` + 右浮 `\text{(式 N)}` 标号。
3. **前向流程列表**:有序列表逐步描述数据如何流动、循环几次、哪些量可导。
4. **模块内部 Mermaid 图**。
5. **该模块的监督信号/损失**(无则写明"该模块无直接监督")。

### 6. 内容深度的四项硬要求(本次统一的核心)

统一格式不只是换皮,而是**四类内容都要有精度下限**。缺任一项,笔记不算完成。

**① 思路(Why)** —— 「研究背景与动机」+「核心洞察」
- 必须回答:此前方法的具体失败模式是什么(不是"效果不好"这种空话)?作者的关键假设是什么?这个假设在什么条件下会失效?
- 必须区分**作者的宣称**与**可验证的证据**:宣称无证据时用 `.callout.warn` + `【推测】` 标注。

**② 算法逻辑(How)** —— 「算法总体框架」+ 逐模块"五件套"
- 公式符号必须与论文原文一致,并给出**符号表**(`$K$: 内参矩阵, `[B, N, 3]` 之类)。
- 数据流必须到**张量维度级**:每步进出形状、循环/迭代次数、可导性与梯度流路径。
- 关键设计要给出"为什么这样做"的机制解释(如"用 1D 窗口是因为该方向近似等距"),而不是复述结构。

**③ 工程细节(Reality)** —— 「⚙️ 工程实现细节」
- 源码/配置文件里的**真实值**:超参、损失权重、特征维度、迭代次数、优化器与 schedule、训练分辨率与 batch、数据规模、依赖版本。**每条都要标出处**(`config/xxx.yaml:12` / `train.py:88`)。
- **资源账**:实测或 README 明示的显存占用、单次迭代/推理延迟、训练总时长、参数量。
- **落地坑**:数据预处理要求(是否依赖 GT depth/相机位姿)、硬件门槛、已知 issue、作者未明说的隐藏约束。
- 拿不到源码时,明确写"无法验证"并标注,禁止用"约""大概"糊过去。

**④ 同类方法对比(Versus)** —— 「🔄 同类方法对比分析」
- **横向对比表**,固定维度(缺失维度写 `—`,不省略列):`方法 / 年份 / 输入假设 / 场景表示 / 监督信号 / 训练数据 / 训练时长 / 推理耗时 / 显存 / 关键指标 / 开源`。
- 至少 3 个同类方法(deep 级 ≥4),且必须包含**论文自己 baseline 之外**的方法——只抄论文对比表等于没做对比。
- 必须给出**对比结论**:逐条说明"它为什么强/弱"的机制原因,而不是罗列数字。
- 必须给出**演进定位表**(`范式 → 代表方法 → 核心先验 → 局限`),把本文放进领域脉络。
- 必须用相对链接交叉引用库内兄弟笔记:`<a href="3D-20260720-MHR.html">MHR</a>`,并在文中点明"与 X 的差异在于…"。

### 7. 「可改进方向分析」章节(知识库特色,deep 级必写)
位于结论前,从三维度分点(每点为 `.card` 带左色条):
- **方法层面**:如"多视角支持名不副实(代码硬编码 lmain/rmain)"、"1D 窗口假设的几何脆弱性"
- **数据与评估**:如"仅在合成数据集验证"、"缺乏时序一致性评估"
- **工程实用性**:如"深度预训练依赖 GT depth"、"anchor loss 权重固定过大"

每点含:① 问题描述 + **代码佐证**(`code` 引用)、② 改进建议。标优先级 `[高优先级]`/`[中优先级]`/`[低优先级]`。末尾加优先级总览表 + "个人认为最关键的三点"。

**关键原则**:改进点要**有代码/实验佐证**,不能空谈。例:说"多视角名不副实"要引用 `EVANet.forward` 中 `torch.stack([lmain, rmain])`;说"伪影在手脚"要引用消融实验原文。

### 8. 更新 papers.json 并重新生成 index.html(**禁止手改 index.html**)
`index.html` 是生成物,由 `build_index.py` 从 `papers.json`(唯一真相源)+ `index_style.css` 生成。新增笔记流程:
1. 在 `papers.json` 的 `notes[]` 对应分区位置追加条目,字段:`file`(笔记文件名)、`title`、
   `section`(卡片分区: `vg-notes-1`/`vg-notes-2`/`vg-notes-3`/`3d`/`sr`/`meta`)、`kind`、`depth`、
   `topics`(从 `topics` 列表中选,可多选)、`meta`(📅 日期/📄 arXiv/🏢 机构等,原文照抄)、
   `tags`、`summary`(一段摘要,可含 `<strong>`)、`links`(`📖 阅读笔记` 本地 + `📄 arXiv` + `💻 GitHub`)。
   **内容必须取自笔记自身 header,禁止编造**(见「二、真实性硬约束」)。
2. 运行 `python build_index.py` 重新生成 `index.html`(编号自动连续,卡片自动带 kind/depth 徽章与筛选数据)。
3. 运行 `python check.py` —— 它会重新生成 index 并与磁盘文件比对,**手改 index.html 会被检出**。

- 页面样式改 `index_style.css`;静态章节(调研总览/对比表/时间线/总结)内容存于 `papers.json` 的 `extra_sections`,同样改后需重新生成。
- **commit 信息规范**:中文、简短——标题一行 + 正文合计不超过 3-4 行,说明改了什么即可,不写多段长文。

---

## 二、真实性硬约束(最高优先级,违反即返工)

本库服务于工程选型决策,**错误信息比没有信息更危险**。以下三条为硬性规定,优先级高于一切排版与模板要求。

### 1. 禁止编造
- **代码**:引用源码必须真实读过(`git clone` 后引用文件路径+行号)。**禁止凭印象编造代码片段、函数名、类名、超参数值**。历史教训:commit `47102e5` 曾清除 AI 编造的代码块。
- **数字**:PSNR/SSIM/FPS/参数量/训练时长/显存等必须来自论文原文(注明表号/章节)或官方源码 README,禁止"估计一个合理值"。历史教训:commit `f4ac3f4` 曾纠正错误的 SOTA 数字。
- **引用**:arXiv ID、作者、机构、会议、日期必须可从 abs 页核实;不存在的链接禁止写入。

### 2. 不确定必须标注(统一格式)
任何无法从原文/源码核实的内容,**必须**用下列统一标记,禁止自由发挥措辞(历史遗留有「不确定/存疑/推测/笔记误差」等 5 种说法,分散且无法检索):

| 标记 | 含义 | 使用场景 |
|---|---|---|
| `【存疑】` | 来源不可靠(口述整理、二手资料、记忆还原) | 同事方案按记忆整理的流程 |
| `【推测】` | 无原文佐证的推断 | "该方法可能使用了……" |
| `【待核实】` | 需后续验证的结论 | 未实际跑通的复现结论 |

- 标记必须**原样包含** `【存疑】`/`【推测】`/`【待核实】` 字样(`check.py` 按此扫描汇总,供定期核实)。
- 建议用 `<span class="uncertain">⚠️【存疑】 ……</span>` 包裹,样式参考 `.highlight.warn`(黄底左条)。
- **「工程实现细节」「同类方法对比」两个章节是标注重灾区**:任何非源码直读的数值(如"约 20 分钟")都必须带标记。

### 3. 改名必须同步全库引用
重命名/移动任何 `.html` 后,按顺序执行:
1. 全库搜索旧文件名(`grep -rn "旧文件名" --include="*.html" --include="*.json" .`),逐一替换为新名;
2. 运行 `python check.py` 确认 0 死链;
3. 通过后才允许 commit。

历史教训:commit `06d83ac` 改名 5 个文件未同步引用,造成全库 48 处断链(commit `91ce20d` 修复)。

---

## 三、文件命名规范

格式:`PREFIX-YYYYMMDD-Name.html`

| Prefix | 领域 | 示例 |
|---|---|---|
| `3D-` | 3D 重建/生成/人体/高斯 | `3D-20241002-EVA-Gaussian.html` |
| `VideoGen-` | 视频生成模型 | `VideoGen-20250326-Wan2.1.html` |
| `ImageGen-` | 图像生成模型(扩散/蒸馏/文生图) | `ImageGen-20240523-DMD2.html` |
| `WorldModel-` | 世界模型(学环境动力学生成多模态内容) | `WorldModel-20260625-Cosmos3.html` |
| `SR-` | 超分辨率/图像修复 | `SR-20250728-HYPIR.html` |
| `Meta-` | 方法论/知识库自身建设 | `Meta-20260813-用AI做论文调研经验分享.html` |

- **Name**:有英文短名用英文(`Wan2.1`、`EVA-Gaussian`、`VGGT`);综述/调研用中文并以 `综述`/`调研`/`思路辨析` 结尾(`前馈3D重建模型综述.html`)。
- **日期**:论文 arXiv 提交日期(如 `20250326`);综述用编撰日期。
- **改名**:重命名后必须同步全库引用并跑 `python check.py`,见「二、真实性硬约束」第 3 条。
- commit `d3239b5 整理知识库命名规范` 确立此规范。

---

## 四、HTML 样式规范(统一高端样式,唯一)

全库**只此一套**。参考实现(可直接复制其 `<style>` 块):`3D-20241002-EVA-Gaussian.html`。

```css
:root{
  --bg:#0f1419; --panel:#1a2029; --panel2:#222b36; --border:#2d3748;
  --text:#e2e8f0; --muted:#94a3b8; --accent:#60a5fa; --accent2:#a78bfa;
  --green:#34d399; --orange:#fb923c; --pink:#f472b6; --red:#f87171;
  --code-bg:#0d1117;
}
```

**必需结构(缺一即不合规)**:
1. `<html lang="zh-CN">` + `<meta charset="UTF-8">` + viewport。
2. **Hero 头**:`.hero`(135° 双层渐变 `#1e3a5f → #2d1b4e → #1a2029` + `::before` 径向光斑)> `.hero-content` > `.badge`(`arXiv:ID · 年 · 类`)+ `h1` + `.subtitle` + `.authors` + `.links`(emoji 徽章链接)。
3. **目录**:`.toc > ol{columns:2;column-gap:40px}`,锚点对应各 `section`。
4. **数字编号标题**:`<h2><span class="num">N</span>标题</h2>`(渐变方块徽章),**不用 emoji 前缀、不用扁平 `.section` 卡片**。
   - 唯一例外:目录区自身标题可写 `<h2>目录</h2>`。
5. **公式**:`.equation` 容器包 `$$...$$`,右浮 `\text{(式 N)}` 标号。
6. **组件**:`.callout`(`.key`/`.warn`/`.tip` 变体)、`.grid-2`/`.grid-3`、`.card`(含 `.num-big`)、`.stage-tag`、`.formula`。
7. **表格**:`th` 用 `--panel2` 底 + `--accent` 字;最优行 `.best`(绿粗)、失败项 `.fail`(红)。
8. **页脚**:`📝 阅读笔记 · 生成于 YYYY-MM-DD · 领域: ...`。

**公式处理(三档,按需选)**
| 档位 | 适用 | 实现 |
|---|---|---|
| MathJax(默认) | 有真公式 | `<script>MathJax={tex:{inlineMath:[['$','$'],['\\(','\\)']],displayMath:[['$$','$$'],['\\[','\\]']]}}</script>` + `tex-svg.js` |
| 纯文本 `.formula` | 公式极少 | Unicode 数学字符(ℒ、‖·‖、×、→) |
| `<code>` 内联 | 仅一两个符号 | 损失写成 `L_FM = E[...]` |

- ⚠️ **已废弃,禁止再用**:NVIDIA 绿扁平主题(`--accent:#76b900`、`.header` + `.section` + emoji 节标题的样式)。存量旧笔记见「八、存量笔记迁移策略」。

---

## 五、外部资源规则

- **CDN 仅限**:`cdn.jsdelivr.net/npm/mermaid@10` 与 `cdn.jsdelivr.net/npm/mathjax@3`。**不用** Bootstrap、Font-Awesome、外部 CSS。
- **资源链接**用 emoji 徽章 + `target="_blank"`:`📄 arXiv abs` / `🌐 arXiv HTML 全文` / `📥 PDF` / `💻 GitHub` / `🤗 HuggingFace` / `🌐 项目主页`。
- **图片**:优先热链 arXiv HTML 图(`loading="lazy"` + `alt`);失效则本地化到 `images/<arxiv-id>/`。**有网络时定期运行 `python check_images.py`** 扫描外链图存活情况,发现失效即本地化(arXiv 图路径绑定版本号,论文出 v2 后旧版图可能消失)。

---

## 六、对比表格规范

- 方法对比:用 ✓/✗/~ 标记,CSS 类 `.check`(绿)/`.cross`(红)/`.partial`(橙);或直接 emoji ✅/❌/~。
- 最优行高亮:`class="best"`(绿粗)或 `.best` 行内高亮;失败项 `class="fail"`(红),如 GPS-Gaussian 在 Δ=90° 标"失败"。
- **「同类方法对比分析」表的维度固定**(见「一.6 ④」),缺失值写 `—`,**不允许删列**。

---

## 七、分析深度分级(depth,新笔记必须声明)

depth 只约束**内容体量**,不约束外观——**外观一律统一高端样式**(「四」)。

| 级别 | 适用 | 章节要求 |
|---|---|---|
| `deep` | 与项目主线强相关、需精读的方法 | 完整模板全部章节 + 源码级「⚙️ 工程实现细节」+ 同类方法对比 ≥4 个 + 「可改进方向分析」(必须有代码/实验佐证) |
| `standard` | 一般论文笔记 | 核心章节齐(1~3、逐模块、损失、实验、消融、同类对比)+ 工程细节可精简 + 同类对比 ≥3 个 + 含「个人分析/核心洞察」 |
| `brief` | 行业全景速查(如闭源模型谱系) | 概要/架构/关键创新/对比速览/资源链接 5 节即可,可省「可改进方向分析」;但**样式仍为高端样式**,对比表维度仍固定 |

- **声明位置**:笔记 Hero 的 `.badge` 或 `.meta` 加 `📊 deep`(或 `standard`/`brief`);`index.html` 卡片 `.tags` 同步标注。
- **定级时机**:立项调研时先定 depth 再动笔,避免把 brief 写成 standard 浪费时间、或把 deep 写成 standard 丢失价值。
- 存量边界(务必注意):`brief` 是**章节裁剪**的许可,不是"可以随便写"的许可——真实性硬约束与四类精度要求对所有 depth 一律生效。
- 存量 62 篇的分级与主题映射记录在 `topics.json`,可作为定级参考。

---

## 八、存量笔记迁移策略(统一格式的落地路径)

历史原因,存量笔记混用两套样式(以 `class="hero"` 判高端样式、`class="header"` 判旧扁平样式)。**不做一次性批量重排**(43 篇一次性重写风险高、易引入编造内容),按以下规则渐进收敛:

1. **新笔记一律按本规范产出**(高端样式 + 四类精度要求)。
2. **旧笔记在被触碰时顺带迁移**:因补内容、修错别字、补对比表而打开某篇旧笔记时,一并把样式升到高端样式(复制 `3D-20241002-EVA-Gaussian.html` 的 `<style>` 块 + 改造头尾),并补齐缺失的必写章节。
3. **纯样式迁移不做内容编造**:迁移时只重排已有内容;迁移中发现的空白章节,要么补真实材料,要么按「二、2」标注,不允许为凑模板而编造。
4. **待迁移清单由脚本输出**:`python check.py` 会报告当前仍为旧样式的笔记篇数与文件列表,可作为迁移进度看板。
5. 迁移后的笔记必须 `python check.py` 通过 + 单独 commit(commit 信息注明"迁移至统一格式",避免与内容新增混在一个提交里)。

---

## 九、验证清单(成稿前)

**硬性项(脚本校验,不通过禁止 commit)**:
- [ ] `python check.py` 通过(0 站内死链、index 全收录、本地图片完整、index 与 papers.json 同步)

**真实性项(见「二、真实性硬约束」)**:
- [ ] 无编造:代码引用有真实文件路径+行号,数字有原文出处(表号/章节/README)
- [ ] 不确定内容已用 `【存疑】`/`【推测】`/`【待核实】` 统一标注(无自由措辞)
- [ ] 「工程实现细节」「同类方法对比」中的非源码直读数值已标注
- [ ] 笔记头部与 index 卡片已声明 depth(deep/standard/brief)

**格式项(统一高端样式,见「四」)**:
- [ ] 使用高端样式:`.hero` + `.toc`(2 列)+ `<h2><span class="num">N</span>`(**无 NVIDIA 绿扁平主题残留**)
- [ ] `<html lang="zh-CN">`,文件名符合 `PREFIX-YYYYMMDD-Name.html`
- [ ] 资源链接含 arXiv abs + HTML 全文 + GitHub(若有)+ 项目主页(若有)
- [ ] 图像有 `loading="lazy"` 与 `alt`;Mermaid/MathJax CDN 与 init 脚本正确
- [ ] 至少 3 张图(总体数据流 / 模块内部 / 训练或推理流程)

**内容项(四项精度要求,见「一.6」)**:
- [ ] 思路:写清先前方法的具体失败模式 + 作者关键假设 + 假设失效条件
- [ ] 算法逻辑:有符号表;数据流到张量维度级;关键设计有机制解释
- [ ] 工程细节:超参/资源账/落地坑有出处;无源码时明确写"无法验证"
- [ ] 同类对比:横向对比表维度齐全 + ≥3 个同类方法(含论文 baseline 以外)
- [ ] 同类对比:有对比结论(机制原因)+ 演进定位表 + 库内兄弟笔记交叉链接
- [ ] 实验表格含 PSNR/SSIM/LPIPS 等量化指标,最优行标 `.best`
- [ ] deep 级笔记含「可改进方向分析」(三维度 + 优先级表 + 代码佐证)

**流程项**:
- [ ] 已更新 `papers.json` 并运行 `python build_index.py` 重新生成 index(禁止直接改 index.html)
- [ ] commit 信息中文、简短(标题 + 正文合计不超过 3-4 行,不写多段长文)
